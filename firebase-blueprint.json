{
  "entities": {
    "Backup": {
      "title": "Backup",
      "description": "A system state snapshot backup containing all ERP data",
      "type": "object",
      "properties": {
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp when the backup was created"
        },
        "label": {
          "type": "string",
          "description": "A short, descriptive label given to the backup"
        },
        "operator": {
          "type": "string",
          "description": "The name of the operator who created the backup"
        },
        "data": {
          "type": "object",
          "description": "A container containing all local tables"
        }
      },
      "required": ["createdAt", "label", "operator", "data"]
    }
  },
  "firestore": {
    "backups/{backupId}": {
      "schema": "Backup",
      "description": "Collection of backup snapshots"
    }
  }
}
