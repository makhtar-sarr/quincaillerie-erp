# Learnings

## Task 1: Git Init

- Initialized Git repository in project root on **Tue Jul 7 12:30 UTC 2026**
- Default branch renamed from `master` to `main` (standard convention)
- Git user configured: `makhtar` / `makhtar@example.com` (local repo config only)
- `.gitignore` already exists with proper exclusions: `node_modules/`, `dist/`, `build/`, `.env*`, etc.
- Repository is clean — no commits yet, all files untracked
- `.gitignore` is untracked (`?? .gitignore`), ready for initial commit
- No remote configured (task requirement)
